---
summary: "mcp-ts-core ^0.10.6: post-pack bundle cleaner, packaging linter checks 8-9, skill syncs, websiteUrl removed from createApp()"
breaking: false
security: false
---

# 0.10.5 — 2026-06-11

## Changed

- **`@cyanheads/mcp-ts-core`** bumped `^0.10.5 → ^0.10.6`; picks up post-pack bundle cleaner (`scripts/clean-mcpb.ts`), packaging linter checks 8 (post-bundle agent-doc entries) and 9 (entrypoint `name`/`title` identity), and template-level `bundle` script update.
- **`package.json` `bundle` script** — appended `&& bun run scripts/clean-mcpb.ts dist/secedgar-mcp-server.mcpb` to strip dependency-shipped agent docs from the packed bundle.
- **`scripts/lint-packaging.ts`** — synced from framework 0.10.6: check 8 (post-bundle `node_modules/**` agent-doc entries via `unzip -Z1`), check 9 (entrypoint `name`/`title` literal identity against unscoped package name), `checkBundleContent` refactored to accept raw string (enables direct unit testing), exports added for cross-module use.
- **`scripts/clean-mcpb.ts`** (new) — post-pack bundle cleaner synced from framework; runs `mcpb clean` then strips `node_modules/**` agent-doc entries unreachable by root-anchored `.mcpbignore` patterns.
- **`createApp()` `websiteUrl`** — removed; not part of the canonical identity block (`name` + `title` only).
- **`skills/orchestrations`** — synced to v1.3 (Phase 4 issue-close pattern: one what-landed comment per issue, then bare close).
- **`skills/polish-docs-meta`** — synced to v2.7 (documents `name`/`title` identity requirement and updated `bundle` script description).
- **`CLAUDE.md` / `AGENTS.md`** — framework version line updated to `^0.10.6`; `bundle` command description updated to reflect the clean step.
