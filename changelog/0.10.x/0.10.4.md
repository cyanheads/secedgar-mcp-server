---
summary: "Server identity: name and title now use the machine name secedgar-mcp-server (was 'SEC EDGAR MCP Server')"
breaking: false
security: false
---

# 0.10.4 — 2026-06-11

## Changed

- **`createApp()`** — passes `name: 'secedgar-mcp-server'` (bare repo name), replacing the previous implicit npm-scoped default; `serverInfo.name` and `server_name` in `/.well-known/mcp.json` now return `secedgar-mcp-server` instead of `@cyanheads/secedgar-mcp-server`.
- **`createApp()` `title`** — changed from `'SEC EDGAR MCP Server'` to `'secedgar-mcp-server'`; the displayed title now matches the machine name.
