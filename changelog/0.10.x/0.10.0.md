---
summary: "secedgar_company_search: ETF/fund ticker resolution, former-name lookup (Facebook→Meta, Square→Block), near-match suggestions on zero-result name queries, and robustness fixes for private/pre-IPO filers"
breaking: false
security: false
---

# 0.10.0 — 2026-06-08

## Added

- **`secedgar_company_search` — ETF/mutual-fund ticker resolution** ([#40](https://github.com/cyanheads/secedgar-mcp-server/issues/40)): `loadTickerCache` now fetches `company_tickers_mf.json` alongside `company_tickers.json` and merges fund symbols (VOO, SCHD, JEPI, etc.) into `byTicker`. Fund results carry `series_id` and `class_id` for downstream scoping. Mirror path resolves fund tickers to CIK; series/class enrichment remains live-path-only.
- **`secedgar_company_search` — former-name resolution** ([#42](https://github.com/cyanheads/secedgar-mcp-server/issues/42)): committed `src/services/edgar/data/former-names.json` asset (generated via `bun run gen:former-names`) maps lowercased prior entity names to zero-padded CIKs. `buildTickerCache` folds them into `allEntries` for name search and the trigram pass, so queries for `Facebook`, `Square`, or `Lucent Technologies` resolve to the current registrant. New `scripts/gen-former-names.ts` generator; `build.ts` now copies `src/**/*.json` into `dist/`.
- **`secedgar_company_search` — near-match suggestions on zero-result name search** ([#41](https://github.com/cyanheads/secedgar-mcp-server/issues/41)): `suggestCompanies(query, allEntries)` runs a Dice-coefficient trigram pass when a name query returns no match, attaching up to 3 ranked candidates to the `no_match` error `data.suggestions` field and the error message. Never auto-resolves.

## Fixed

- **Operating-company ticker precedence**: on the rare cross-file ticker collision (e.g. SPCX appears in both `company_tickers.json` and `company_tickers_mf.json`), the operating-company entry now wins over the fund entry. ([#40](https://github.com/cyanheads/secedgar-mcp-server/issues/40))
- **Private/pre-IPO filer robustness**: `format()` in `company-search.tool.ts` now tolerates `null` `exchanges` elements and a `null` fiscal year end, so filers without exchange listings or a fiscal year end (e.g. SpaceX) return a valid result instead of a `-32007` validation error. ([#40](https://github.com/cyanheads/secedgar-mcp-server/issues/40))
