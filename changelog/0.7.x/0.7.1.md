---
summary: "Empty strings now rejected for required string fields — company/concept on secedgar_get_financials and concept/period on secedgar_fetch_frames return a clean ValidationError instead of reaching the handler."
breaking: false
security: false
---

# 0.7.1 — 2026-05-30

## Fixed

- **`secedgar_get_financials`** — `company` and `concept` now carry `.min(1)`; empty strings return `MCP error -32602 ValidationError` before the handler runs. ([#24](https://github.com/cyanheads/secedgar-mcp-server/issues/24))
- **`secedgar_fetch_frames`** — `concept` and `period` now carry `.min(1)`; same validation gate. ([#24](https://github.com/cyanheads/secedgar-mcp-server/issues/24))
